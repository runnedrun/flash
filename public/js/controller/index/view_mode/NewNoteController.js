NewNoteController = function() {
  var self = this;

  self.create = function(content, hint) {
    console.log("creating!");
//    var newNotePromise = NoteManager.create(content, hint);
//    newNotePromise.then(function(note) {
//      console.log("new note!", note);
//    })
  }
}